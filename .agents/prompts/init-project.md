# Init project (fallback stub)

Use this only when the harness has no command input or does not read
`AGENTS.md` before the first message. Paste it as the first message:

---

Read `.agents/commands/agents-init.md` and execute it exactly. It initializes
this project for the `.agents/` kit. Then follow `.agents/AGENTS.md` for all
subsequent work.
