# Resume work

Paste this into the agent after a break or a new session, in a project already
initialized with the kit:

---

You are resuming work in a repository governed by `.agents/`. Read
`.agents/context.md`, then the most recent artifacts in `docs/00_adr/`,
`docs/01_implementation_plans/`, `docs/02_change_plans/`, and
`docs/03_test_plans/` (the highest numbers are the newest). Identify which
gate is currently awaiting approval. Summarize the state in 1 to 3 sentences
and wait for instructions. Do not start new work and do not pass any gate.
